description "pxlapp"

start on (filesystem)
stop on runlevel [016]

respawn
setuid www-data
setgid www-data
chdir {{ django_home }}

env DATABASE_URL={{ db_url }}

exec gunicorn -b 127.0.0.1:8000 -w 4 pxl.wsgi --error-logfile /tmp/gunicorn-error.log